publish = false                                                                   # disables crates.io publish
push = true                                                                       # pushes commit + tag
tag = true
sign-tag = true                                                                   # optional
tag-message = "chore: release version {{version}}"
pre-release-commit-message = "chore: release version {{version}}"
